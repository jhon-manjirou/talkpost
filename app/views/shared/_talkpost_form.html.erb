<%= form_with model: @talkpost do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "今日は天気が良いですね", size: "50" %>
  </div>
  <%= f.submit "投稿する", class: "btn btn-primary" %>
<% end %>